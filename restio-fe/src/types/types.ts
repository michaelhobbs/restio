/**
 * This file contains any custom types which need to be shared across components.
 */

// This enum is derived from the generated API, to avoid having literal types declared everywhere.
export enum UserRoles {
    'User' = 'User',
    'Owner' = 'Owner',
    'Admin' = 'Admin',
}
